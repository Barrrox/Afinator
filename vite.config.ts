import { defineConfig } from 'vite'

export default defineConfig({
  // O 'base' deve ser o nome do seu repositório no GitHub entre barras
  // Ex: se o link for github.com/usuario/Afinator, o base é '/Afinator/'
  base: '/Afinator/', 
})